# Управление инбаундами

## Обзор

Бот теперь автоматически подтягивает все инбаунды из вашей панели 3x-ui. Вы можете выбрать, какие из них будут доступны для подключения пользователей.

## Как это работает

### 1. Настройка активных инбаундов

**Команда:** `/settings`

При выполнении этой команды:
- Бот подключается к панели 3x-ui
- Получает список всех существующих инбаундов
- Показывает их в виде списка с кнопками

**Пример отображения:**
```
⚙️ Настройки инбаундов

Выберите инбаунды, которые будут доступны для подключения пользователей.

✅ - Включен
⚪ - Выключен

✅ Main VLESS (vless:443)
⚪ Backup VMess (vmess:8443)
✅ Shadowsocks (shadowsocks:8388)
```

**Действия:**
- Нажмите на инбаунд, чтобы включить/выключить его
- Статус меняется мгновенно (✅/⚪)
- Используйте кнопку "🔄 Обновить список" для синхронизации с панелью

### 2. Одобрение заявок с выбором инбаунда

Когда пользователь запрашивает доступ:

1. Вам приходит уведомление с информацией о пользователе
2. Нажимаете "✅ Одобрить"
3. Бот показывает список **только активных** инбаундов:
   ```
   🔹 Выберите инбаунд для подключения:
   
   🔹 Main VLESS (vless:443)
   🔹 Shadowsocks (shadowsocks:8388)
   ```
4. Выбираете нужный инбаунд
5. Клиент автоматически создается в выбранном инбаунде

### 3. Автоматическая генерация конфигурации

После одобрения:
- Пользователь получает конфигурацию для **конкретного инбаунда**
- QR-код и ссылка формируются с правильным портом
- Все параметры берутся из выбранного инбаунда

## База данных

### Новая таблица: `active_inbounds`

Хранит информацию о выбранных инбаундах:
- `inbound_id` - ID из 3x-ui
- `remark` - Название инбаунда
- `protocol` - Протокол (VLESS, VMess, etc.)
- `port` - Порт
- `is_enabled` - Включен ли для выбора

### Обновленная таблица: `users`

Добавлено поле:
- `inbound_id` - ID инбаунда, к которому подключен пользователь

## Преимущества

✅ **Гибкость**: Можно использовать разные инбаунды для разных пользователей
✅ **Безопасность**: Распределение нагрузки между инбаундами
✅ **Удобство**: Не нужно вручную вводить ID инбаундов
✅ **Актуальность**: Список всегда синхронизирован с панелью 3x-ui

## Типичные сценарии использования

### Сценарий 1: Разные протоколы для разных устройств

```
✅ VLESS-TLS (для ПК и Android)
✅ Shadowsocks (для iOS)
⚪ VMess (резервный, выключен)
```

При одобрении заявки от пользователя iOS выбираете Shadowsocks.

### Сценарий 2: Балансировка нагрузки

```
✅ Server-1 VLESS (vless:443)
✅ Server-2 VLESS (vless:8443)
```

Распределяете пользователей между двумя серверами.

### Сценарий 3: Тестирование нового инбаунда

```
✅ Main VLESS (основной)
✅ Test VLESS (тестовый)
```

Подключаете новых пользователей к тестовому инбаунду для проверки.

## Частые вопросы

**Q: Что если я изменю настройки инбаунда в панели 3x-ui?**
A: Нажмите "🔄 Обновить список" в `/settings` для синхронизации.

**Q: Можно ли переключить пользователя на другой инбаунд?**
A: Пока нет, но это можно сделать вручную через панель 3x-ui или добавить в будущих версиях.

**Q: Что если я выключу инбаунд, к которому подключены пользователи?**
A: Существующие пользователи продолжат работать. Инбаунд просто не будет доступен для новых подключений.

**Q: Нужно ли указывать XUI_INBOUND_ID в .env?**
A: Нет, это поле больше не требуется. Все инбаунды выбираются через `/settings`.

## Миграция со старой версии

Если вы обновляетесь со старой версии:

1. Удалите `XUI_INBOUND_ID` из `.env` (опционально)
2. Запустите бота - база данных обновится автоматически
3. Выполните `/settings` для выбора активных инбаундов
4. Готово!

Существующие пользователи продолжат работать, но у них не будет привязки к конкретному инбаунду. Новые пользователи будут создаваться с правильной привязкой.
