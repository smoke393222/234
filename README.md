# VPN Bot для управления 3x-ui

Telegram-бот для управления VPN-панелью 3x-ui, ориентированный на приватное использование друзьями.

[![Docker](https://img.shields.io/badge/docker-ready-blue.svg)](https://www.docker.com/)
[![Python](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

**Особенности:**
- 🚀 Готов к развертыванию на VPS за 5 минут
- 🔗 Автоматическое получение готовых ссылок из 3x-ui API
- 🎯 Поддержка VLESS (REALITY, TLS), VMess, Trojan
- ⚙️ Гибкое управление инбаундами
- 📊 Статистика трафика в реальном времени
- 🐳 Docker и Docker Compose из коробки

## Возможности

### Для пользователей:
- 📝 Запрос доступа к VPN через бота
- 👤 Личный кабинет с отображением статистики трафика
- 🔗 **Автоматическое получение готовых ссылок из 3x-ui API** (VLESS, VMess, Trojan)
- 🎯 Полная поддержка REALITY, TLS, WebSocket, gRPC и других конфигураций
- 📱 QR-коды для быстрого подключения
- 📖 Инструкции по подключению для разных платформ

### Для администратора:
- ⚙️ **Выбор активных инбаундов из панели 3x-ui** (автоматическая загрузка)
- ✅ Одобрение/отклонение заявок с выбором инбаунда для каждого пользователя
- 👥 Управление пользователями (активация/деактивация)
- 🗑 Удаление пользователей
- 📊 Просмотр статистики использования в реальном времени

## Технологический стек

- **Python 3.10+**
- **Aiogram 3.x** - фреймворк для Telegram ботов
- **SQLAlchemy** - ORM для работы с базой данных
- **SQLite** - база данных
- **HTTPX** - асинхронный HTTP клиент для API 3x-ui
- **Loguru** - логирование
- **Pydantic Settings** - управление конфигурацией
- **QRCode** - генерация QR-кодов

## Структура проекта

```
vpn_bot/
├── bot/
│   ├── handlers/          # Обработчики команд
│   ├── keyboards/         # Клавиатуры бота
│   ├── middlewares/       # Middleware
│   └── main.py           # Точка входа
├── core/
│   ├── config.py         # Конфигурация
│   └── logger.py         # Настройка логирования
├── services/
│   └── xui_client.py     # API клиент для 3x-ui
├── database/
│   ├── models.py         # Модели БД
│   ├── database.py       # Подключение к БД
│   └── repositories.py   # Репозитории
├── utils/
│   ├── qr_generator.py   # Генерация QR-кодов
│   └── formatters.py     # Форматирование данных
├── data/                 # База данных (создается автоматически)
├── logs/                 # Логи (создаются автоматически)
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── .env                  # Конфигурация (создать из .env.example)
```

## 🚀 Быстрый старт

### На VPS сервере:

```bash
# 1. Установите Docker
curl -fsSL https://get.docker.com | sh

# 2. Создайте директорию
mkdir -p /opt/vpn-bot && cd /opt/vpn-bot

# 3. Загрузите файлы проекта
# (через git clone или scp)

# 4. Настройте конфигурацию
cp env.example .env
nano .env  # Заполните BOT_TOKEN, ADMIN_TG_ID, XUI_*

# 5. Запустите
chmod +x deploy.sh && ./deploy.sh
```

**Готово!** Откройте бота в Telegram и отправьте `/start`

Подробная инструкция: [INSTALL.md](INSTALL.md) или [DEPLOYMENT.md](DEPLOYMENT.md)

---

## Установка и настройка

### Предварительные требования

1. VPS сервер с Ubuntu/Debian
2. Docker и Docker Compose
3. Рабочая панель 3x-ui с настроенным инбаундом
4. Telegram бот токен (получить у @BotFather)
5. Ваш Telegram ID (узнать у @userinfobot)

### Шаг 1: Подготовка

```bash
# Подключитесь к VPS
ssh root@your-server-ip

# Установите Docker (если нужно)
curl -fsSL https://get.docker.com | sh

# Создайте директорию
mkdir -p /opt/vpn-bot && cd /opt/vpn-bot

# Загрузите файлы проекта
# Вариант A: через Git
git clone https://your-repo/vpn-bot.git .

# Вариант B: через SCP с локального компьютера
# scp -r * root@your-server-ip:/opt/vpn-bot/
```

### Шаг 2: Настройка конфигурации

```bash
# Создайте файл конфигурации
cp env.example .env
nano .env
```

### Шаг 2: Настройка переменных окружения

Отредактируйте файл `.env`:

```env
# Токен бота от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID (администратор)
ADMIN_TG_ID=123456789

# URL вашей панели 3x-ui (с портом!)
XUI_BASE_URL=https://your-server.com:2053

# Данные для входа в панель
XUI_USERNAME=admin
XUI_PASSWORD=your_password

# Проверка SSL (false для самоподписанных сертификатов)
XUI_VERIFY_SSL=false

# Настройки для генерации VLESS ссылок (ОПЦИОНАЛЬНО - используются как fallback)
# Бот автоматически получает готовые ссылки из 3x-ui API
# Эти настройки нужны только если API не вернет ссылку (крайне редко)
VLESS_SERVER=your-server.com  # IP или домен сервера (опционально)
VLESS_PORT=443                # Порт (опционально)
VLESS_SNI=your-server.com     # SNI (опционально)
```

### Шаг 3: Запуск

**Вариант A: Автоматический (рекомендуется)**

```bash
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматически:
- Проверит конфигурацию
- Создаст необходимые директории
- Соберет и запустит Docker контейнер
- Покажет логи и статус

**Вариант B: Ручной**

```bash
# Сборка и запуск
docker compose up -d --build

# Просмотр логов
docker compose logs -f

# Остановка
docker compose down
```

### Шаг 4: Первичная настройка

1. Откройте бота в Telegram
2. Отправьте `/settings`
3. Выберите инбаунды из списка
4. Отметьте нужные галочками ✅

Готово! Бот настроен и готов к работе.

## Использование

### Для пользователей

1. Найдите бота в Telegram
2. Нажмите `/start`
3. Нажмите "📝 Запросить доступ"
4. Введите свое имя
5. Дождитесь одобрения администратора
6. После одобрения используйте:
   - **👤 Мой профиль** - просмотр статистики трафика
   - **🔗 Подключиться** - получение ссылки и QR-кода
   - **📖 Инструкции** - инструкции по подключению для разных платформ

### Для администратора

1. **Настройка инбаундов** (первый запуск):
   - Используйте `/settings`
   - Бот загрузит список инбаундов из 3x-ui
   - Выберите инбаунды, которые будут доступны для подключения
   - Отметьте нужные галочками ✅

2. **Одобрение заявок**:
   - При новой заявке вам придет уведомление
   - Нажмите "✅ Одобрить"
   - Выберите инбаунд для подключения пользователя
   - Клиент автоматически создается в 3x-ui
   - Пользователь получит уведомление

3. **Управление пользователями** (`/admin`):
   - Просмотр списка всех пользователей
   - Просмотр статистики трафика
   - Активация/деактивация доступа
   - Удаление пользователей

---

## Управление на сервере

```bash
# Просмотр логов в реальном времени
docker compose logs -f

# Просмотр статуса
docker compose ps

# Перезапуск бота
docker compose restart

# Остановка бота
docker compose down

# Обновление бота
./update.sh

# Бэкап базы данных
cp data/vpn_bot.db backups/vpn_bot_$(date +%Y%m%d).db

# Просмотр использования ресурсов
docker stats vpn_bot
```

## API 3x-ui

Бот использует следующие эндпоинты 3x-ui API:

- `POST /login` - аутентификация
- `GET /panel/api/inbounds/list` - получение списка всех инбаундов
- `GET /panel/api/inbounds/get/{id}` - получение конфигурации инбаунда
- `POST /panel/api/inbounds/addClient` - добавление клиента
- `GET /panel/api/inbounds/getClientTraffics/{email}` - статистика трафика
- `POST /panel/api/inbounds/updateClient/{uuid}` - обновление клиента
- `POST /panel/api/inbounds/{id}/delClient/{uuid}` - удаление клиента

### Генерация ссылок

Бот **автоматически генерирует готовые ссылки подключения** на основе конфигурации инбаунда из API:

- **VLESS**: полная поддержка REALITY, TLS, WebSocket, gRPC, TCP
- **VMess**: поддержка TLS, WebSocket
- **Trojan**: поддержка TLS

Все параметры (публичные ключи, fingerprint, SNI, пути, заголовки) берутся из конфигурации инбаунда.

## Логирование

Логи сохраняются в директории `logs/`:
- Ротация каждый день в полночь
- Хранение логов 30 дней
- Автоматическое сжатие старых логов

Уровни логирования:
- `INFO` - основные события (по умолчанию)
- `DEBUG` - детальная информация
- `ERROR` - ошибки

## База данных

Используется SQLite с двумя таблицами:

**users:**
- Информация о пользователях
- UUID для связи с 3x-ui
- Статусы одобрения и активности

**access_requests:**
- История заявок на доступ
- Статусы обработки
- Информация об администраторе, обработавшем заявку

## Безопасность

- Доступ к административным функциям только для указанного ADMIN_TG_ID
- Все пароли и токены хранятся в переменных окружения
- Логи не содержат чувствительных данных
- Автоматическая переаутентификация при истечении сессии 3x-ui

## Поддерживаемые VPN клиенты

- **Android:** V2RayNG
- **Windows/Linux:** Nekoray
- **iOS:** Streisand

## Устранение неполадок

### Бот не может подключиться к 3x-ui

**Ошибка:** `All connection attempts failed` или `Login failed`

**Решение:**

1. **Проверьте конфигурацию в `.env`:**
   ```bash
   nano .env
   ```
   Убедитесь, что:
   - `XUI_BASE_URL` правильный (с протоколом и портом)
   - `XUI_USERNAME` и `XUI_PASSWORD` верные
   - `XUI_VERIFY_SSL=false` для самоподписанных сертификатов

2. **Запустите тест подключения:**
   ```bash
   docker exec -it vpn_bot python test_xui_connection.py
   ```

3. **Проверьте доступность панели:**
   ```bash
   curl -k https://your-server.com:2053/login
   ```

4. **Смотрите детальное руководство:** [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

### Бот не запускается

1. Проверьте правильность BOT_TOKEN
2. Убедитесь, что все переменные окружения заданы
3. Проверьте логи: `docker compose logs`

### Ошибки при создании клиента

1. Проверьте доступность панели 3x-ui (XUI_BASE_URL)
2. Проверьте правильность XUI_USERNAME и XUI_PASSWORD
3. Убедитесь, что выбраны активные инбаунды через `/settings`
4. Проверьте логи бота для деталей

### Не генерируется QR-код

1. Проверьте правильность VLESS_SERVER, VLESS_PORT, VLESS_SNI
2. Убедитесь, что библиотека qrcode установлена с поддержкой PIL

## 📚 Документация

Полная документация доступна в следующих файлах:

- **[INSTALL.md](INSTALL.md)** - Быстрая установка за 5 минут
- **[DEPLOYMENT.md](DEPLOYMENT.md)** - Полное руководство по развертыванию на VPS
- **[INBOUND_MANAGEMENT.md](INBOUND_MANAGEMENT.md)** - Управление инбаундами
- **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** - Устранение неполадок и диагностика
- **[CHECKLIST.md](CHECKLIST.md)** - Чеклист для проверки установки
- **[SUMMARY.md](SUMMARY.md)** - Итоговое резюме и технические детали
- **[DOCS_INDEX.md](DOCS_INDEX.md)** - Навигация по всей документации

### Быстрые ссылки:
- [Как установить на VPS?](INSTALL.md)
- [Как настроить инбаунды?](INBOUND_MANAGEMENT.md)
- [Как обновить бота?](DEPLOYMENT.md#обновление-бота)
- [Как сделать бэкап?](DEPLOYMENT.md#резервное-копирование)
- [Устранение проблем](DEPLOYMENT.md#устранение-неполадок)

---

## Будущие улучшения

- [ ] Лимиты трафика для пользователей
- [ ] Статистика использования по дням/месяцам
- [ ] Автоматические уведомления при достижении лимита
- [ ] Экспорт статистики в CSV/Excel
- [ ] Web-интерфейс для администратора
- [ ] Поддержка нескольких администраторов

---

## Лицензия

MIT License

---

## Автор

Разработано для приватного использования среди друзей.

---

## Поддержка

При возникновении проблем:

1. Проверьте [CHECKLIST.md](CHECKLIST.md)
2. Посмотрите логи: `docker compose logs`
3. Проверьте конфигурацию: `cat .env`
4. Убедитесь в доступности панели 3x-ui
5. Прочитайте раздел [Устранение неполадок](DEPLOYMENT.md#устранение-неполадок)

Если проблема не решена, создайте Issue с описанием проблемы и логами.

---

## Благодарности

- [3x-ui](https://github.com/MHSanaei/3x-ui) - панель управления Xray
- [Aiogram](https://github.com/aiogram/aiogram) - фреймворк для Telegram ботов
- Всем контрибьюторам проекта

---

**Удачного использования! 🚀**
