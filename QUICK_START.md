# Быстрый старт VPN Bot

## Минимальная настройка для запуска

### 1. Получите необходимые данные

**Telegram Bot:**
- Создайте бота у [@BotFather](https://t.me/BotFather)
- Получите токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Ваш Telegram ID:**
- Узнайте у [@userinfobot](https://t.me/userinfobot)

**Данные 3x-ui панели:**
- URL панели (например: `https://your-server.com`)
- Логин и пароль администратора
- ID инбаунда (найдите в панели, обычно это 1)

### 2. Настройте конфигурацию

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните:

```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_TG_ID=ваш_telegram_id

XUI_BASE_URL=https://ваш-сервер.com
XUI_USERNAME=admin
XUI_PASSWORD=ваш_пароль

# VLESS настройки опциональны - бот получает ссылки из API
# VLESS_SERVER=ваш-сервер.com
# VLESS_PORT=443
# VLESS_SNI=ваш-сервер.com
```

### 3. Запустите бота

**С Docker (рекомендуется):**

```bash
docker-compose up -d
```

**Без Docker:**

```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или: source venv/bin/activate  # Linux/Mac

pip install -r requirements.txt
python -m bot.main
```

### 4. Проверьте работу

1. Найдите бота в Telegram
2. Отправьте `/settings` для настройки инбаундов
3. Выберите инбаунды, которые будут доступны (отметьте галочками ✅)
4. Отправьте `/start` от другого аккаунта
5. Запросите доступ
6. Проверьте, что вам как админу пришло уведомление
7. Одобрите заявку, выбрав инбаунд
8. Проверьте генерацию QR-кода

## Структура проекта

```
vpn_bot/
├── bot/                    # Telegram бот
│   ├── handlers/          # Обработчики команд
│   │   ├── user.py       # Пользовательские команды
│   │   └── admin.py      # Административные команды
│   ├── keyboards/        # Клавиатуры
│   ├── middlewares/      # Middleware
│   └── main.py          # Точка входа
├── core/                 # Ядро приложения
│   ├── config.py        # Конфигурация
│   └── logger.py        # Логирование
├── services/            # Сервисы
│   └── xui_client.py   # API клиент 3x-ui
├── database/           # База данных
│   ├── models.py      # Модели
│   ├── database.py    # Подключение
│   └── repositories.py # Репозитории
└── utils/             # Утилиты
    ├── qr_generator.py # QR-коды
    └── formatters.py   # Форматирование
```

## Основные команды

### Для пользователей:
- `/start` - Начать работу с ботом
- `📝 Запросить доступ` - Отправить заявку
- `👤 Мой профиль` - Посмотреть статистику
- `🔗 Подключиться` - Получить конфиг и QR-код
- `📖 Инструкции` - Инструкции по подключению

### Для администратора:
- `/settings` - Настройка активных инбаундов (выбор из панели 3x-ui)
- `/admin` - Панель управления пользователями
- Одобрение/отклонение заявок с выбором инбаунда
- Активация/деактивация пользователей
- Удаление пользователей

## Логи

Логи сохраняются в `logs/bot.log` с ротацией каждый день.

Просмотр логов:
```bash
# Docker
docker-compose logs -f

# Без Docker
tail -f logs/bot.log
```

## Частые проблемы

**Бот не отвечает:**
- Проверьте правильность BOT_TOKEN
- Убедитесь, что бот запущен

**Ошибка при одобрении заявки:**
- Проверьте доступность 3x-ui панели
- Проверьте правильность XUI_USERNAME и XUI_PASSWORD
- Убедитесь, что XUI_INBOUND_ID существует

**Не генерируется QR-код:**
- Проверьте настройки VLESS_SERVER, VLESS_PORT, VLESS_SNI

## Поддержка

Проверьте логи для диагностики проблем:
- Все действия логируются
- Ошибки API отображаются с деталями
- Время выполнения запросов записывается

## Безопасность

- Храните `.env` в секрете
- Не публикуйте токены и пароли
- Регулярно проверяйте логи на подозрительную активность
- Используйте сильные пароли для 3x-ui панели
