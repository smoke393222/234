# Часто задаваемые вопросы (FAQ)

## Установка и настройка

### Q: Какие требования к VPS серверу?

**A:** Минимальные требования:
- 1 vCPU
- 512 MB RAM
- 10 GB Disk
- Ubuntu 20.04+ или Debian 10+

Бот очень легковесный и потребляет ~100-150 MB RAM.

---

### Q: Нужно ли устанавливать Python на сервере?

**A:** Нет! Бот работает в Docker контейнере, где уже есть все необходимое. Достаточно установить только Docker.

---

### Q: Можно ли запустить бота на том же сервере, где установлен 3x-ui?

**A:** Да, это даже рекомендуется! В этом случае используйте `XUI_BASE_URL=http://localhost:порт` в конфигурации.

---

### Q: Какой порт использует 3x-ui по умолчанию?

**A:** Обычно это порт 2053 или 54321. Проверьте в настройках вашей панели 3x-ui.

---

### Q: Бот не запускается, что делать?

**A:** Проверьте:
1. Логи: `docker compose logs`
2. Конфигурацию: `cat .env`
3. Что Docker установлен: `docker --version`
4. Что контейнер запущен: `docker compose ps`

Если в логах есть ошибки, они подскажут проблему.

---

## Конфигурация

### Q: Где взять BOT_TOKEN?

**A:** 
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен, который выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

---

### Q: Где узнать свой Telegram ID?

**A:** Отправьте любое сообщение боту [@userinfobot](https://t.me/userinfobot), он ответит вашим ID.

---

### Q: Нужно ли заполнять VLESS_SERVER, VLESS_PORT и другие VLESS параметры?

**A:** Нет, это опционально! Бот автоматически получает все параметры из 3x-ui API. Эти настройки используются только как fallback, если API не вернет ссылку (крайне редко).

---

### Q: Что такое XUI_BASE_URL и как его правильно указать?

**A:** Это адрес вашей панели 3x-ui.

Примеры:
- Если 3x-ui на том же сервере: `http://localhost:2053`
- Если 3x-ui на другом сервере: `https://your-panel.com:2053`
- Если используется домен: `https://panel.example.com`

---

## Инбаунды

### Q: Что такое инбаунд?

**A:** Инбаунд (inbound) - это входящее подключение в 3x-ui панели. Каждый инбаунд имеет свой протокол (VLESS, VMess, Trojan), порт и настройки.

---

### Q: Зачем выбирать активные инбаунды?

**A:** Это позволяет:
- Скрыть технические/тестовые инбаунды от пользователей
- Выбирать разные инбаунды для разных пользователей
- Управлять доступными протоколами

---

### Q: Можно ли назначить разные инбаунды разным пользователям?

**A:** Да! При одобрении заявки вы выбираете инбаунд для конкретного пользователя.

---

### Q: Как обновить список инбаундов?

**A:** Просто откройте `/settings` снова - бот автоматически загрузит актуальный список из 3x-ui.

---

## Использование

### Q: Пользователь не получает ссылку подключения

**A:** Проверьте:
1. Пользователь одобрен (статус "Одобрен" в `/admin`)
2. Пользователь активен (не деактивирован)
3. У пользователя назначен инбаунд
4. Логи бота: `docker compose logs | grep ERROR`

---

### Q: Ссылка не работает в VPN клиенте

**A:** Проверьте:
1. Правильность протокола (VLESS/VMess/Trojan)
2. Что инбаунд активен в 3x-ui панели
3. Что порт открыт в файрволе
4. Что сервер доступен извне

Попробуйте подключиться к серверу: `ping your-server.com`

---

### Q: Как посмотреть статистику трафика пользователя?

**A:** 
- Для пользователя: кнопка "👤 Мой профиль"
- Для админа: `/admin` → выбрать пользователя → "ℹ️ Информация"

---

### Q: Можно ли ограничить трафик пользователя?

**A:** В текущей версии нет, но это в планах на будущее. Пока можно настроить лимиты в самой панели 3x-ui.

---

## Управление

### Q: Как деактивировать пользователя?

**A:** 
1. Отправьте `/admin`
2. Выберите пользователя
3. Нажмите "🔴 Деактивировать"

Пользователь не сможет подключаться, но его данные сохранятся.

---

### Q: Как полностью удалить пользователя?

**A:** 
1. Отправьте `/admin`
2. Выберите пользователя
3. Нажмите "🗑 Удалить"
4. Подтвердите удаление

Пользователь будет удален из бота И из 3x-ui панели.

---

### Q: Как сделать резервную копию базы данных?

**A:** 
```bash
cp data/vpn_bot.db backups/vpn_bot_$(date +%Y%m%d).db
```

Для автоматического бэкапа настройте cron (см. [DEPLOYMENT.md](DEPLOYMENT.md#резервное-копирование)).

---

### Q: Как восстановить базу данных из бэкапа?

**A:** 
```bash
docker compose down
cp backups/vpn_bot_YYYYMMDD.db data/vpn_bot.db
docker compose up -d
```

---

## Обновление

### Q: Как обновить бота до новой версии?

**A:** 
```bash
cd /opt/vpn-bot
./update.sh
```

Или вручную:
```bash
git pull  # если используете Git
docker compose down
docker compose up -d --build
```

---

### Q: Сохранятся ли данные пользователей при обновлении?

**A:** Да! База данных хранится в отдельном volume и не удаляется при обновлении. Но рекомендуется делать бэкап перед обновлением.

---

## Безопасность

### Q: Безопасно ли хранить пароли в .env файле?

**A:** Да, если:
1. Файл имеет права 600: `chmod 600 .env`
2. Доступ к серверу защищен (SSH ключи, файрвол)
3. Файл не добавлен в Git (он в .gitignore)

---

### Q: Может ли кто-то кроме админа получить доступ к админским функциям?

**A:** Нет! Доступ к админским функциям имеет только пользователь с Telegram ID, указанным в `ADMIN_TG_ID`.

---

### Q: Что делать если токен бота утек?

**A:** 
1. Отзовите старый токен через @BotFather: `/revoke`
2. Создайте новый токен: `/newbot` или `/token`
3. Обновите `BOT_TOKEN` в `.env`
4. Перезапустите бота: `docker compose restart`

---

## Производительность

### Q: Сколько пользователей может обслуживать бот?

**A:** Бот легковесный и может обслуживать сотни пользователей на минимальном VPS. Ограничение скорее в 3x-ui панели, чем в боте.

---

### Q: Как очистить старые логи?

**A:** 
```bash
# Удалить логи старше 30 дней
find logs/ -name "*.log" -mtime +30 -delete

# Или настроить автоматическую очистку через cron
```

Бот уже настроен на ротацию логов (30 дней хранения).

---

### Q: Как уменьшить использование диска?

**A:** 
```bash
# Очистить Docker кэш
docker system prune -a

# Удалить старые бэкапы
find backups/ -name "*.db" -mtime +30 -delete

# Очистить старые логи
find logs/ -name "*.log" -mtime +7 -delete
```

---

## Протоколы

### Q: Какие протоколы поддерживает бот?

**A:** 
- ✅ VLESS (REALITY, TLS, WebSocket, gRPC, TCP)
- ✅ VMess (TLS, WebSocket)
- ✅ Trojan (TLS)

Все параметры берутся автоматически из конфигурации инбаунда.

---

### Q: Что такое REALITY?

**A:** REALITY - это новый протокол обфускации для VLESS, который маскирует трафик под обычный HTTPS. Бот полностью поддерживает REALITY со всеми параметрами (публичный ключ, fingerprint, SNI, shortId, spiderX).

---

### Q: Можно ли использовать несколько протоколов одновременно?

**A:** Да! Создайте несколько инбаундов в 3x-ui с разными протоколами, активируйте их в боте через `/settings`, и назначайте нужный протокол каждому пользователю при одобрении.

---

## Устранение проблем

### Q: Бот не подключается к 3x-ui

**A:** Проверьте:
1. Доступность панели: `curl -k http://localhost:2053`
2. Правильность логина/пароля
3. Что панель запущена
4. Логи бота: `docker compose logs | grep "3x-ui"`

---

### Q: Ошибка "Client not found" при получении ссылки

**A:** Это значит, что клиент не создан в 3x-ui. Проверьте:
1. Пользователь одобрен
2. В логах нет ошибок при создании клиента
3. Клиент существует в панели 3x-ui

---

### Q: QR-код не генерируется

**A:** Проверьте логи: `docker compose logs | grep qr`. Обычно это связано с проблемами генерации ссылки.

---

### Q: База данных повреждена

**A:** Восстановите из бэкапа:
```bash
docker compose down
cp backups/vpn_bot_YYYYMMDD.db data/vpn_bot.db
docker compose up -d
```

---

## Разное

### Q: Можно ли использовать бота для коммерческих целей?

**A:** Бот разработан для приватного использования среди друзей. Для коммерческого использования рекомендуется добавить функции биллинга, лимитов и т.д.

---

### Q: Как добавить еще одного администратора?

**A:** В текущей версии поддерживается только один администратор. Поддержка нескольких администраторов в планах на будущее.

---

### Q: Можно ли изменить язык бота?

**A:** Бот на русском языке. Для добавления других языков нужно реализовать систему локализации (в планах).

---

### Q: Где хранятся данные бота?

**A:** 
- База данных: `data/vpn_bot.db`
- Логи: `logs/bot.log`
- Конфигурация: `.env`

Все это в директории проекта (`/opt/vpn-bot`).

---

### Q: Как связаться с разработчиком?

**A:** Создайте Issue в репозитории GitHub с описанием вопроса или проблемы.

---

## Дополнительная помощь

Если вы не нашли ответ на свой вопрос:

1. Проверьте [CHECKLIST.md](CHECKLIST.md)
2. Прочитайте [DEPLOYMENT.md](DEPLOYMENT.md) - раздел "Устранение неполадок"
3. Посмотрите логи: `docker compose logs`
4. Создайте Issue в репозитории

---

**Последнее обновление:** 2026-01-21
