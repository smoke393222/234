# Чеклист развертывания VPN бота

Используйте этот чеклист для проверки правильности установки.

---

## Перед установкой

- [ ] У вас есть VPS сервер с SSH доступом
- [ ] Сервер работает на Ubuntu/Debian/CentOS
- [ ] У вас есть root доступ или sudo права
- [ ] Панель 3x-ui установлена и работает
- [ ] У вас есть логин и пароль от панели 3x-ui
- [ ] В 3x-ui настроен хотя бы один инбаунд
- [ ] Вы создали Telegram бота через @BotFather
- [ ] Вы знаете свой Telegram ID (через @userinfobot)

---

## Установка

- [ ] Подключились к VPS через SSH
- [ ] Установили Docker: `docker --version`
- [ ] Установили Docker Compose: `docker compose version`
- [ ] Создали директорию: `/opt/vpn-bot`
- [ ] Загрузили файлы проекта
- [ ] Создали файл `.env` из `env.example`
- [ ] Заполнили `BOT_TOKEN` в `.env`
- [ ] Заполнили `ADMIN_TG_ID` в `.env`
- [ ] Заполнили `XUI_BASE_URL` в `.env`
- [ ] Заполнили `XUI_USERNAME` в `.env`
- [ ] Заполнили `XUI_PASSWORD` в `.env`
- [ ] Запустили: `./deploy.sh` или `docker compose up -d --build`

---

## Проверка работы

- [ ] Контейнер запущен: `docker compose ps` показывает "Up"
- [ ] Нет ошибок в логах: `docker compose logs | grep ERROR`
- [ ] Бот отвечает на `/start` в Telegram
- [ ] Бот показывает главное меню администратору

---

## Первичная настройка

- [ ] Отправили `/settings` боту
- [ ] Увидели список инбаундов из 3x-ui
- [ ] Выбрали нужные инбаунды (отметили галочками)
- [ ] Получили подтверждение "Настройки сохранены"

---

## Тестирование

- [ ] Создали тестовую заявку (от другого аккаунта или попросили друга)
- [ ] Получили уведомление о новой заявке
- [ ] Одобрили заявку, выбрав инбаунд
- [ ] Пользователь получил уведомление об одобрении
- [ ] Пользователь может нажать "🔗 Подключиться"
- [ ] Пользователь получил ссылку и QR-код
- [ ] Ссылка начинается с `vless://`, `vmess://` или `trojan://`
- [ ] QR-код сканируется в VPN клиенте
- [ ] Подключение работает

---

## Управление пользователями

- [ ] Команда `/admin` показывает список пользователей
- [ ] Можно просмотреть информацию о пользователе
- [ ] Можно деактивировать пользователя
- [ ] Можно активировать пользователя
- [ ] Можно удалить пользователя
- [ ] Статистика трафика отображается корректно

---

## Безопасность

- [ ] Файл `.env` имеет права 600: `chmod 600 .env`
- [ ] Настроен файрвол (UFW или iptables)
- [ ] Открыты только необходимые порты (SSH, 3x-ui)
- [ ] Используются SSH ключи вместо паролей
- [ ] Регулярно обновляется система: `apt update && apt upgrade`

---

## Резервное копирование

- [ ] Создана директория для бэкапов: `mkdir -p backups`
- [ ] Настроен автоматический бэкап через cron
- [ ] Проверен ручной бэкап: `cp data/vpn_bot.db backups/test.db`
- [ ] Проверено восстановление из бэкапа

---

## Мониторинг

- [ ] Знаете как смотреть логи: `docker compose logs -f`
- [ ] Знаете как проверить статус: `docker compose ps`
- [ ] Знаете как перезапустить: `docker compose restart`
- [ ] Знаете как обновить: `./update.sh`

---

## Документация

- [ ] Прочитали `README.md`
- [ ] Прочитали `INSTALL.md` или `DEPLOYMENT.md`
- [ ] Прочитали `INBOUND_MANAGEMENT.md`
- [ ] Знаете где искать помощь при проблемах

---

## Автозапуск

- [ ] Бот автоматически запускается после перезагрузки сервера
- [ ] Проверено: `sudo reboot` → подождать → `docker compose ps`

---

## Производительность

- [ ] Проверено использование ресурсов: `docker stats vpn_bot`
- [ ] CPU < 10%
- [ ] RAM < 200 MB
- [ ] Нет утечек памяти (проверить через несколько дней)

---

## Готово к продакшену

Если все пункты отмечены ✅, ваш бот готов к использованию!

---

## Полезные команды для справки

```bash
# Логи
docker compose logs -f
docker compose logs --tail=100
docker compose logs | grep ERROR

# Управление
docker compose ps
docker compose restart
docker compose down
docker compose up -d

# Обновление
./update.sh

# Бэкап
cp data/vpn_bot.db backups/vpn_bot_$(date +%Y%m%d).db

# Восстановление
docker compose down
cp backups/vpn_bot_YYYYMMDD.db data/vpn_bot.db
docker compose up -d

# Мониторинг
docker stats vpn_bot
du -sh data/vpn_bot.db
du -sh logs/

# Очистка
docker system prune -a
find logs/ -name "*.log" -mtime +30 -delete
find backups/ -name "*.db" -mtime +30 -delete
```

---

**Удачи! 🚀**
